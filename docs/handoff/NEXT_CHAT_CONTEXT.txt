COPY/PASTE THIS INTO A NEW CHAT

PROJECT: WineShopPOS / barcode company

You are continuing development of an existing WineShopPOS barcode scanner + POS + inventory application. Do NOT restart architecture planning and do NOT ask the user to repeat existing setup. Treat the following as canonical project context.

USER WORKING STYLE
- User develops in VS Code on Windows.
- Project path: E:\\WineShopPOS ; Git Bash path: /e/WineShopPOS
- Use Git Bash/Linux-style commands, NOT PowerShell.
- Keep chat responses concise. For large code changes, create one downloadable .sh script instead of pasting hundreds of lines.
- Each significant change should: update code -> npm run build -> update docs/tests -> git commit/push -> deploy if relevant.
- User wants beginner-friendly exact steps and minimal unnecessary explanation.

REPOSITORY / HOSTING
- GitHub: https://github.com/saifsiddiqui59/WineShopPOS.git
- Branch: main
- Confirmed final application commit during this project: 4a33fbb - Chapter 15 - Supabase live data roles and cloud-ready POS
- Azure subscription: Azure subscription 1
- Resource group: wineshopPOS
- Storage account: wineshoppos
- Azure region: Central India
- Production static URL: https://wineshoppos.z29.web.core.windows.net/
- Frontend is React/Vite and uses HashRouter for Azure Blob static hosting.

SUPABASE
- Project name: WineShopPOS
- Project ID/ref: uiurgplnsgmawvxhjzzp
- Region: ap-northeast-1
- Project URL: https://uiurgplnsgmawvxhjzzp.supabase.co
- Main schema file: supabase_multi_shop_schema.sql
- Edge Function: supabase/functions/manage-shop-users/index.ts
- Shop ID: 5c94dbca-9bb5-451e-831a-8cfa42d06013
- Current Shop Admin Auth UUID: 6304882e-d97f-4628-9933-997c4038194f
- Current Shop Admin email: mdsaif72496@gmail.com
- NEVER request or expose service-role/secret key. A service-role key was exposed earlier; it must be rotated if not already. Browser uses only Supabase publishable/anon key from .env.local.

CURRENT ARCHITECTURE
Barcode scanner (keyboard/HID) -> React/Vite SPA -> Supabase Auth -> Supabase PostgreSQL/RLS/RPCs.
Shop user creation uses Supabase Edge Function manage-shop-users so service-role stays server-side.
Azure Blob hosts only the static frontend. Supabase is the live source of truth for business data.

MULTI-SHOP / SUBSCRIPTION MODEL
- shops table contains access_enabled, subscription_status, subscription_end_date, max_users.
- Every business table is shop_id scoped.
- Platform Owner/Developer is separate from shop roles and controls the first Shop ADMIN and subscription kill switch.
- Shop ADMIN can create MANAGER/CASHIER only, not another ADMIN.
- MANAGER: products, inventory, purchases, reports, POS, sales.
- CASHIER: dashboard, POS, intended own sales view.
- Kill switch: shops.access_enabled=false and subscription_status='SUSPENDED'.

DATABASE TABLES
shops, platform_admins, profiles, shop_settings, categories, suppliers, products, inventory, purchases, purchase_items, sales, sale_items, payments, stock_movements, stock_adjustments, shop_counters.

CORE RPCs
- create_new_product(): product + opening inventory + opening stock movement.
- receive_purchase(): purchase + items + inventory increase + stock movement.
- adjust_stock(): controlled manual stock adjustment + audit.
- complete_sale(): validates stock, creates sale/items/payment, reduces inventory, records movements.

FEATURE STATUS
DONE:
- React/Vite UI shell
- Dashboard
- Product master
- Barcode POS
- Cash/UPI/Card + discount/reference
- Supabase products/inventory/purchases/sales/payments
- Receive stock with cases + loose bottles
- Stock adjustments
- Sales/invoice view
- Reports
- Supabase Auth
- ADMIN/MANAGER/CASHIER role-aware frontend
- Admin creates Manager/Cashier through secure Edge Function
- Multi-shop data model
- Subscription kill switch
- Azure Blob static deployment
- Git documentation/test files

NOT DONE / NEXT:
- OCR invoice stock receiving is NOT implemented. Purchases page only documents a placeholder/roadmap. Recommended: Azure AI Document Intelligence invoice model -> extract lines -> match products -> human review -> receive_purchase(). Never let OCR directly change inventory.
- Dedicated 80mm receipt printer integration.
- Returns/refunds.
- Automated subscription billing.
- Stronger production RLS hardening.

IMPORTANT SECURITY HARDENING TO DO NEXT
Current RLS tenant isolation exists, but some SELECT policies are broader than intended role permissions. UI hiding is not enough. Before strict production:
1. Cashier sales SELECT should be restricted to sales.cashier_id = auth.uid().
2. sale_items/payments should inherit corresponding sale visibility.
3. purchases/purchase_items and supplier purchase data should be ADMIN/MANAGER only.
4. Cashier must not be able to query products.purchase_price directly; create a safe view/RPC or tighten DB design.
5. Add platform audit log + MFA for privileged users.

TEST DATA
- Scanner test barcode: 8900000010016
- Expected demo product: Kingfisher Strong 650ml
- Seed contains ~50 Indian-market products.

FINAL SMOKE TEST FOR ANY CHANGE
1. npm run build.
2. Admin login.
3. Scan 8900000010016.
4. Complete sale; stock decreases.
5. Receive purchase; stock increases.
6. Add/edit/deactivate product.
7. Create/login Cashier; restricted menus.
8. Reports/Sales load.
9. If Edge Function changed, deploy it.
10. Git commit/push and Azure deploy when frontend changes.

DOCUMENTATION
The user has/should place these generated documents into Git:
- WineShopPOS_Developer_Handbook.docx
- WineShopPOS_User_Manual.docx
- Markdown companions
- docs/ chapters and testing files
The developer handbook contains full generated scripts/SQL appendices.

When continuing: preserve current working cloud MVP. Prefer small, safe incremental changes with rollback/testing.

<!-- WSP_AI_VERIFIED_V1_START -->
## AI Owner Assistant V1 — VERIFIED PRODUCTION MILESTONE

**Verified:** 2026-08-30  
**Status:** Production path working end-to-end  
**Feature:** Ask WineShopPOS  
**Tier:** PRO  
**Current access:** ADMIN / Owner Center

### Verified production topology

```text
WineShopPOS React/Vite
        ↓ existing Supabase user session
Supabase Auth access token
        ↓
Azure Function /api/ai/chat
Central India — Consumption Y1
        ↓ system-assigned managed identity
Microsoft Foundry project
South India
        ↓
WineShopPOS-Owner-Agent
gpt-5-mini
        ↓ controlled function calls
Caller-scoped Supabase AI RPCs
        ↓
Grounded business answer
```

### Current Azure configuration

- Resource group: `wineshopPOS`
- Static website storage: `wineshoppos` — Central India
- Document Intelligence: `wineshoppos-docintel-45b7d2b9` — Central India — F0
- AI Function App: `wineshoppos-ai-1a61d5885c`
- AI Function region: Central India
- AI Function plan: Consumption `Y1`
- Always On/Premium/Dedicated hosting: not used for AI V1
- Production Foundry account: `wineshoppos-ai-in-1a61d5885c`
- Foundry region: South India
- Foundry project: `wineshoppos-ai`
- Model deployment: `gpt-5-mini`
- Model version verified during deployment: `2025-08-07`
- Model SKU: `GlobalStandard`
- Logical agent: `WineShopPOS-Owner-Agent`
- Foundry invocation: current `agent_reference` request shape

### Runtime identity / RBAC

The Azure Function uses its **system-assigned managed identity** for Foundry access.

The working runtime uses `AIProjectClient` plus project-level Responses/Conversations APIs. For this architecture, the Function managed identity requires **Foundry User at the Foundry project scope**. The narrower Agent Consumer role alone was insufficient and produced HTTP 403 during the project-level runtime call.

This role is scoped to the WineShopPOS Foundry project, not the whole subscription.

### Supabase / tenant authorization

- Supabase project ref: `uiurgplnsgmawvxhjzzp`
- AI migration: `20260830070000_ai_owner_assistant_v1.sql` — applied
- Organization boundary: `organizations.id`
- Shop boundary: `shops.id`
- User/shop authorization: `user_shop_memberships`
- Production UI does **not** use a fixed ADMIN email, shop ID, organization ID or role.
- React sends the currently logged-in Supabase user's access token.
- Azure Function validates the caller and dynamically resolves authorized shop scope.
- Tenant/shop isolation is enforced programmatically, not by prompting the model.

### Verified business tools

1. `ai_get_sales_summary`
2. `ai_get_profit_summary`
3. `ai_get_inventory_health`
4. `ai_get_reorder_recommendations`
5. `ai_get_supplier_price_history`
6. `ai_get_product_stock_history`
7. `ai_get_shift_variances`
8. `ai_get_audit_exceptions`
9. `ai_get_expense_summary`

There is no unrestricted SQL/database-query tool and no MCP/RAG/vector database in AI V1.

### Verification evidence

The following were verified successfully:

- Supabase ADMIN authentication
- dynamic ADMIN shop membership resolution
- `ai_resolve_context`
- `ai_rate_limit_check`
- `ai_log_activity`
- all nine business RPCs
- direct Foundry `agent_reference` invocation
- Foundry function-call loop
- multiple business tool calls in one question
- function-call outputs returned to Foundry
- final grounded AI answer
- Azure Function hosted in Central India on Consumption Y1
- Function managed identity access to the South India Foundry project
- production `/api/ai/chat` path after the Foundry project RBAC correction

A verified test for **“How is my shop performing today?”** used multiple tools and returned the correct current business facts for the test shop.

### Legacy cloud cleanup

The earlier East US Foundry resource is **not the production AI resource**. Production uses the South India account listed above. The East US Foundry resource may be removed after the final cleanup review confirms no dependency remains.

### Observability/evaluation status

- Application Insights exists for the AI Function App.
- Function health and failure telemetry are available.
- Full Foundry trace connection, production AI quality evaluation, dashboards and automated quality gates are the **next AI operational-hardening milestone**.
- Do not claim continuous Foundry evaluation is already enabled until that work is completed.

<!-- WSP_AI_VERIFIED_V1_END -->

V3 API AUTOMATION CURRENT STATE
V3_API_AUTOMATION_20260831
Branch: V3
Feature commit: 7dd99f21e03e79b17490c837cb1bd5d470823dca
Preview: https://wspv35c9453b6e9a1.z29.web.core.windows.net/
Private invoice storage: wspinv5c9453b6e9a1
Standalone invoice API: https://wsp-v3-invoice-53b6e9a1.azurewebsites.net
Invoice API runtime: Windows Consumption / Node.js 24
Supabase invoice-automation-ingest: DEPLOYED
Shop UUID: 5c94dbca-9bb5-451e-831a-8cfa42d06013
Sample central Gmail: wineshoppos.invoice.app@gmail.com
Sample sender: wineshoppos.sample.shop@gmail.com
Email Logic App: NOT DEPLOYED — real OAuth authorization required. Failed unauthorised Gmail API connection/workflow resource was removed. The later local sender-normalization patch also stopped before preview/commit because its expected source marker was absent; no deployed runtime changed. Active ARM template accepts only an EXISTING authorized Gmail connection resource ID. Consumer @gmail.com with HTTP/Blob requires BYOA OAuth; Google Workspace is the alternative. Main/production static frontend unchanged. Next: review preview → authorize real Email → deploy Logic App → test one invoice → explicit V3→main merge. WhatsApp remains V3-01B.
